{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.maxigirl123/fortik",
  "description": "Real-time merchant verification for agentic commerce. One call before the payment settles — checks storefront legitimacy so AI agents don't pay fraudulent merchants.",
  "repository": {
    "url": "https://github.com/maxigirl123/Fortik",
    "source": "github"
  },
  "version": "0.3.2",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "fortik",
      "version": "0.3.2",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "GOOGLE_SAFE_BROWSING_API_KEY",
          "description": "Google Safe Browsing API key for scam domain checks. Optional — degrades gracefully if not set.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "SAM_GOV_API_KEY",
          "description": "SAM.gov API key for US federal exclusions check. Optional — skipped if not set.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "URLHAUS_AUTH_KEY",
          "description": "URLhaus Auth-Key for malware URL lookups. Optional — degrades gracefully if not set.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        }
      ]
    }
  ]
}
